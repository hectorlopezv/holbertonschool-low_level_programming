#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
int b;
for (i = '0'; i <= '9'; i++)
{
putchar(i);
}
for (b = 'a'; b <= 'f'; b++)
{
putchar(i);
}
putchar(' ');
return (0);
}
