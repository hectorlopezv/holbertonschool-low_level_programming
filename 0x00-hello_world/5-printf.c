#include <stdio.h>
/**
  * main - Entry point
  *
  * Return: Always 0 (Sucess)
  */
int main(void)
{
char str2[] = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", str2);
return (0);
}
